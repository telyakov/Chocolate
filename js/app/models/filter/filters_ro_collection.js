/**
 * @class
 * @extends Backbone.Collection
 */
var FiltersROCollection = (function (Backbone) {
    'use strict';
    return Backbone.Collection.extend(
        /**
         * @lends FiltersROCollection
         */
        {
    });
})(Backbone);